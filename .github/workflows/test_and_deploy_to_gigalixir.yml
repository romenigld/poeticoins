name: Test and Deploy
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        otp: [24.1]
        elixir: [1.13.2]
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-elixirt@v1
      with:
        otp-version: ${{ matrix.otp }}
        elixir-version: ${{ matrix.elixir }}
    - name: Setup Node
      uses: actions/setup-node@v1
      with:
        node-version: 15.8.0

    - name: Install deps
      run: |
        mix deps.get
        npm install --prefix assets
    - run: mix test
